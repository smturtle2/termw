# server bind — default 127.0.0.1, expose via reverse proxy at user discretion
HOST=127.0.0.1
PORT=3000
SHELL=/usr/bin/zsh
HOME=/root
# optional overrides — theme is read from config/theme.json, not env
# THEME_MODE=light
# LANG=ko_KR.UTF-8
# LC_ALL=ko_KR.UTF-8
